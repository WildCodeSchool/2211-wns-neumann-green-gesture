import { extendTheme } from "@chakra-ui/react";

const colors = {
  primary: {
    main: "#12B886",
  },
  secondary: "#F6B203",
  tertiary: "#554CBB",
  accent: "#007DFF",
};

const theme = extendTheme({ colors });

export default theme;
